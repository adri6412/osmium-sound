<?xml version="1.0" encoding="utf-8"?>
<!-- One VU meter offered by the store (AppliancePlaybackActivity). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="12dp">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <ImageView
            android:id="@+id/vu_store_preview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:importantForAccessibility="no"
            android:visibility="gone" />
        <TextView
            android:id="@+id/vu_store_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="6dp"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:background="@color/hifiGold"
            android:textColor="@android:color/black"
            android:textSize="12sp"
            android:visibility="gone" />
    </FrameLayout>

    <TextView
        android:id="@+id/vu_store_name"
        style="@style/ApplianceTitle"
        android:layout_marginTop="6dp" />

    <TextView
        android:id="@+id/vu_store_details"
        style="@style/ApplianceHelp"
        android:layout_marginBottom="2dp" />

    <TextView
        android:id="@+id/vu_store_error"
        style="@style/ApplianceHelp"
        android:textColor="#F0B4B4"
        android:visibility="gone" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/vu_store_action"
        style="@style/Widget.Material3.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</LinearLayout>

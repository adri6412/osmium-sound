# Osmium Sound — keep the tailnet out of the boot's way.
# See smbd.service.d/hifi-no-boot-block.conf for why. Measured: 2.3s.
# Remote access is not something the on-screen interface waits for.
[Unit]
DefaultDependencies=no
After=basic.target network-online.target
